extends ../layouts/app.pug

block content
  div
    a(href='/') Main page
  div
    table(class="table")
        tr
            th Title
        for post in posts
          tr
              td 
                a(href=`/posts/${post.id}`) #{post.title}
