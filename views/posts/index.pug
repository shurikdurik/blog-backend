extends ../layouts/app.pug

block content
  div
    a(href='/') Main page
    table(class="table")
        tbody
        tr
            th Title
            for post in posts
            tr
                td 
                a(href=`/posts/${post.id}`) #{post.title} 